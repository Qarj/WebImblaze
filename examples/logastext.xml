<testcases repeat="1">

<!--
The logastext parameter will put <logastext> and </logastext> tags around the http log entry for
the current test step.

These parameters are used by the WebInject Framework to tell the framework to display the response
as raw text, rather than as html markup.
-->

<case
    id="10"
    description1="Get Home page"
    method="get"
    url="http://{WIC}"
    verifypositive="automated testing examples"
    logastext="true"
/>

</testcases>